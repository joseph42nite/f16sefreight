# Coordinate cropping for the AWB path — extract_awb_new.py crops boxes with this.
pdfplumber==0.11.9

# The text layer for /extract-unstructured. 10x pdfplumber on a one-page invoice, with
# identical output — measured, not assumed. pdfplumber stays because it does a different
# job (cropping) that it does well.
pymupdf==1.26.5
fastapi==0.136.1
uvicorn==0.46.0
python-multipart==0.0.28

# Schema validation for the model step. PRD §5.1 constrains the model to a schema AND
# validates what comes back — validation alone detects a malformed payload, it does not
# prevent one.
pydantic==2.13.5
